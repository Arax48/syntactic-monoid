# Runtime (visualizacion opcional, el nucleo del proyecto no la requiere)
graphviz>=0.20      # Wrapper de Python sobre el binario `dot` (Graphviz).
matplotlib>=3.7     # Tablas de Cayley y diagramas de clases.

# Generador de las hojas informativas estaticas en web/ejemplos/
markdown>=3.5

# Pruebas y calidad
pytest>=8.0
pytest-cov>=4.1
